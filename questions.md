# Вопросы на проработку

1. Транзакция - это примитив языка или БД?
2. Можно ли не описывая транзакцию в языке программирования, заставить БД выполнять запросы в транзакции
3. Можно ли не использовать транзакцию, но добиться той же функциональности средствами языка?
   А средствами БД?
   Если можно, зачем тогда использовать транзакцию, если можно обойтись без неё?
4. Если мы не будем использовать транзакции, к чему это может привести?
5. Как часто мы пользуемся функцией отмены выполнения ранее выполненных запросов?
6. На сколько сильно транзакция ухудшает производительность? Если, конечно, вообще ухудшает
7. Как работает Rollback? Он создаёт запросы, которые отменяют предыдущие запросы?
8. В чём разница между statement и prepared statement?
   Если их использовать в транзакции, она по разному будет с ними работать или одинаково?
   А при откате транзакции?
9. Что такое ReadOnly? Почему нужна эта опция?
   Можно ли её реализовать с использованием разных уровней изоляции?
10. Сколько уровней изоляции существует? Чем они отличаются друг от друга?
    Почему нельзя сделать один уровень изоляции и использовать его всегда?
11. Можно ли сделать транзакцию без изоляции данных?
    Если можно, будет ли такая транзакция влиять на производительность?
12. Какой уровень изоляции больше всего снижает производительность выполнения запросов?
    Каков механизм этого снижения?
13. Опиши аномалии с понятными и конкретными примерами: Lost Update (Потерянное обновление), Missing or Double Read (Потерянное или двойное чтение), Dirty Read (Грязное чтение), Non-Repeatable Read (Не повторяющееся чтение), Phantom Read (Фантомное чтение), Write-Skew #0 Anomaly , Write-Skew #1 Anomaly , Write-Skew #2 Anomaly , Read-Skew Anomaly
14. Пишем код на go. Открываем транзакцию, пишем 10 разных операций, закрываем транзакцию.
    Верно ли утверждение, что каждая операция с сервера go будет отправляться на БД отдельным запросом по сети?
    Можно ли настроить два разных режима, в одном из которых каждая операция будет отправляться отдельным запросом
    А в другом все операции будут отправляться одним запросом?
15. Допустим, у нас есть 5 огромных операций на модификации данных.
    Предположим, что каждая операция модифицирует 5 000 000 строк в 5 разных таблицах.
    Выполнение очередной операции зависит от выполнения предыдущей.
    В коде между операциями у нас есть бизнес логика.
    Верно ли утверждение, что мы будем выполнять каждую операцию в отдельном запросе?
    Если да, то сможем ли мы откатить транзакцию, если у нас произойдёт разрыв сети с БД?
16. Можно ли сделать вложенные друг в друга транзакции?
    Зачем такое может быть нужно? Почему нельзя сделать отдельные транзакции, не помещая их во внешнюю?
17. Можно ли настроить внешнюю транзакцию так, чтобы она откатывалась, если любая из вложенных транзакции упала с ошибкой?
    Зачем тогда нам в этом случае вложенные транзакции? Почему нельзя оставить одну внешнюю?
18. Допустим у нас есть 3 приложения. Они взаимодействуют друг с другом.
    В первое приложение приходит запрос. Первое приложение выполняет операции с БД, делая при этом запросы в другие приложения
    Другие приложения в рамках обработки запросов также делают операции с БД.
    Как нам написать транзакцию так, чтобы она откатывала все изменения, сделанные разными приложениями?
    Будет ли реализация такой транзакции меняться, если у нас каждое приложение пишет не в одну БД, а в разные?
    А в разные схемы в рамках одной БД?
20. Будет ли работать транзакция, если мы делаем в рамках неё операции, которые меняют данные в разных схемах?
    А в случае linked server?
21. На основании чего стоит выбирать уровень изолированности транзакции?
    Можно ли сделать в рамках одной транзакции вложенные транзакции с разным уровнем изолированности?
22. Какой уровень изолированности в PostgreSQL по умолчанию?
    Можно ли в настройках БД поменять этот уровень по умолчанию?
    Если в go у нас один уровень изолированности по умолчанию, а в БД другой,
    то с каким уровнем изолированности будут выполняться опреации - с тем, что в go, или с тем, что в БД
23. Можно ли настроить в БД уровень изолированности транзакций так, чтобы он использовался в любом случае,
    независимо от того, какой приходит в запросе от приложения?
24. Можно ли в БД отключить транзакции в принципе?
    Возникнет ли ошибка, если мы из приложения отправим запрос с ипользованием транзакции?
25. Можно ли по логам БД понять, что тот или иной запрос выполнялся в транзакции? Или это невозможно?
26. Можно ли отключить транзакции только для одной таблицы в БД?
    Как тогда будут выполняться транзакции, если они используют несколько таблиц в одной операции?